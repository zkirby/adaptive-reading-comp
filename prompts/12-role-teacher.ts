/**
 * Instructing the AI to perform the role of a teacher.
 * TODO: investigate passing this in the `system` message instead.
 *
 * @attribute Role
 */
export const prompt = `Please rewrite the following passage as if you were a world renowned teacher trying to help a student understand the subject matter.`;
