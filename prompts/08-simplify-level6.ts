/**
 * Attempting to re-write a passage for someone at a 6th grade reading level.
 *
 * Note: I'm being careful to avoid biasing the model (such as by saying "Please simplify the..") with any further
 * direction than just asking for a re-write.
 *
 * @attribute Reading level
 */
export const prompt = `Please rewrite the following passage for someone with a 6th grade reading level`;
